# Tokenizer config shared by train/train_tokenizer.py and eval/tokenize_test.py.
# Vocab size and output dir are inherited from the selected experiment; t,
# train_chars, and output_file have standalone defaults that the sweep
# overrides per cell.

vocab_size: ${experiment.vocab_size}

# Transition point. Special cases:
#   t == vocab_size      -> only Phase 1 runs (standard BPE w/ whitespace pretok)
#   t == 0               -> only Phase 2 runs (BPE w/o whitespace pretok)
#   0 < t < vocab_size   -> SuperBPE (Phase 1 to t, Phase 2 extends to vocab_size)
t: ${tokenizer.vocab_size}

# Training corpus (used by train_tokenizer.py; ignored by tokenize_test.py).
input_file: ${data.output_dir}/${data.train_file}
train_chars: 1e8                  # Unicode codepoints prefix of input_file

output_dir: ${experiment.tokenizers_dir}
output_file: t${tokenizer.t}.json

overwrite: false
